1. SELECT COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість книг`, AVG(price) AS `Середня вартість книг`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop;
2. SELECT COUNT(*) AS `Загальна кількість книг` FROM bookshop WHERE price IS NOT NULL;
3. SELECT new, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість`, AVG(price) AS `Середня вартість`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop GROUP BY new;
4. SELECT YEAR(dates) AS `Рік видання`, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість`, AVG(price) AS `Середня вартість`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop GROUP BY YEAR(dates);
5. SELECT YEAR(dates) AS `Рік видання`, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість книг`, AVG(price) AS `Середня вартість книг`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop WHERE price < 10 OR price > 20 GROUP BY YEAR(dates);
6. SELECT YEAR(dates) AS `Рік видання`, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість книг`, AVG(price) AS `Середня вартість книг`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop GROUP BY YEAR(dates) ORDER BY `Загальна кількість книг` DESC;
7. SELECT COUNT(*) AS `Загальна кількість кодів книг`, COUNT(DISTINCT code) AS `Кількість унікальних кодів книг` FROM bookshop;
8. SELECT SUBSTRING(name, 1, 1) AS `Перша буква назви`, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість книг` FROM bookshop GROUP BY SUBSTRING(name, 1, 1) ORDER BY `Загальна кількість книг` DESC;
9. SELECT SUBSTRING(name, 1, 1) AS `Перша буква`, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість книг` FROM bookshop WHERE name REGEXP '^[^A-Za-z0-9]' GROUP BY `Перша буква`;
10. SELECT SUBSTRING(name, 1, 1) AS `Перша буква`, COUNT(*) AS `Загальна кількість книг`, SUM(price) AS `Загальна вартість книг` FROM bookshop WHERE name NOT REGEXP '^[A-Za-z0-9]' AND YEAR(dates) > 2000 GROUP BY `Перша буква` ORDER BY `Загальна кількість книг` DESC;
11. SELECT SUBSTRING(name, 1, 1) AS `Перша буква`, COUNT(*) AS `Загальна кількість`, SUM(price) AS `Загальна вартість` FROM bookshop GROUP BY `Перша буква` ORDER BY `Перша буква` DESC;
12. SELECT YEAR(dates) AS `Рік`, MONTH(dates) AS `Місяць`, COUNT(*) AS `Загальна кількість`, SUM(price) AS `Загальна вартість`, AVG(price) AS `Середня вартість`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop GROUP BY `Рік`, `Місяць` ORDER BY `Рік` ASC, `Місяць` ASC;
13. SELECT YEAR(dates) AS `Рік`, MONTH(dates) AS `Місяць`, COUNT(*) AS `Загальна кількість`, SUM(price) AS `Загальна вартість`, AVG(price) AS `Середня вартість`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop WHERE dates IS NOT NULL GROUP BY `Рік`, `Місяць` ORDER BY `Рік` ASC, `Місяць` ASC;
14. SELECT YEAR(dates) AS `Рік`, MONTH(dates) AS `Місяць`, COUNT(*) AS `Загальна кількість`, SUM(price) AS `Загальна вартість`, AVG(price) AS `Середня вартість`, MIN(price) AS `Мінімальна ціна`, MAX(price) AS `Максимальна ціна` FROM bookshop WHERE dates IS NOT NULL GROUP BY YEAR(dates), MONTH(dates) ORDER BY YEAR(dates) DESC, MONTH(dates) ASC;
15. SELECT CASE WHEN `new` = 'Yes' THEN 'Новинка' ELSE 'Не новинка' END AS `NeW`, SUM(price) AS `Загальна ціна`, SUM(price * 36.9) AS `Загальна ціна в грн.`, SUM(price * 0.94) AS `Загальна ціна в євро` FROM bookshop WHERE dates IS NOT NULL GROUP BY `NeW`;
16. SELECT CASE WHEN `new` = 'Так' THEN 'Новинка' ELSE 'Не новинка' END AS `NeW`, SUM(ROUND(price, 2)) AS `Загальна ціна`, SUM(ROUND(price * 36.9, 2)) AS `Загальна ціна в грн.`, SUM(ROUND(price * 0.94, 2)) AS `Загальна ціна в євро` FROM bookshop WHERE dates IS NOT NULL GROUP BY `NeW`;
17. SELECT publishinghouse, COUNT(*) AS Загальна_кількість_книг, SUM(price) AS Загальна_вартість, AVG(price) AS Середня_вартість, MIN(price) AS Мінімальна_ціна, MAX(price) AS Максимальна_ціна FROM bookshop GROUP BY publishinghouse;
18. SELECT theme, publishinghouse, COUNT(*) AS загальна_кількість, SUM(price) AS загальна_вартість, AVG(price) AS середня_вартість, MIN(price) AS мінімальна_ціна, MAX(price) AS максимальна_ціна FROM bookshop GROUP BY theme, publishinghouse ORDER BY publishinghouse;
19. SELECT category, theme, publishinghouse, COUNT(*) AS загальна_кількість, SUM(price) AS загальна_вартість, AVG(price) AS середня_вартість, MIN(price) AS мінімальна_ціна, MAX(price) AS максимальна_ціна FROM bookshop GROUP BY category, theme, publishinghouse ORDER BY publishinghouse, theme, category;
20. SELECT DISTINCT publishinghouse FROM bookshop WHERE ROUND(price / pages, 2) > 0.10;